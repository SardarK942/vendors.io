# Archived components

Old implementations kept around in case we want to restore them.

| File         | Replaced by                                                     | Replaced in                  |
| ------------ | --------------------------------------------------------------- | ---------------------------- |
| `Navbar.tsx` | `src/components/ui/BaazarChrome.tsx` (wrapping `StaggeredMenu`) | `feat/staggered-menu-chrome` |

To restore, `git mv` the file back to its original path and re-wire the layouts.
