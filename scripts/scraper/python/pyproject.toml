[project]
name = "baazar-scraper-sidecar"
version = "0.1.0"
description = "ScrapeGraphAI sidecar for sub-project K"
requires-python = ">=3.12"
dependencies = [
  "scrapegraphai>=1.0.0",
  "anthropic>=0.40.0",
  "playwright>=1.50.0",
  "pydantic>=2.0",
]

[tool.uv]
package = false
